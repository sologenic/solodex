{
  "name": "solodex",
  "version": "1.0.7",
  "description": "",
  "main": "build/src/index.js",
  "module": "build/src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "standard-version",
    "build": "rm -rf ./build && npx tsc",
    "build:add": "npm run version && npm run build && git add -A"
  },
  "standard-version": {
    "skip": {
      "commit": true
    }
  },
  "pre-commit": [
    "build:add"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.5",
    "pre-commit": "^1.2.2",
    "standard-version": "^9.5.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "dayjs": "^1.11.7"
  }
}
