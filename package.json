{
  "name": "solodex-wallet-sdk",
  "version": "1.0.20",
  "description": "",
  "main": "dist/main/src/index.js",
  "module": "dist/module/src/index.js",
  "scripts": {
    "test": "ts-node test.ts",
    "watch": "npx tsc --watch",
    "build": "rm -rf ./dist && npm run build:main && npm run build:module",
    "version": "standard-version",
    "publish:npm": "npm run version && npm run build && npm publish",
    "build:main": "npx tsc -p tsconfig.json",
    "build:module": "npx tsc -p tsconfig.module.json",
    "build:add": "npm run build && npm run version && git add -A"
  },
  "standard-version": {
    "skip": {
      "commit": true
    }
  },
  "pre-commit": [
    "build:add"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.16.9",
    "@types/ws": "^8.5.4",
    "pre-commit": "^1.2.2",
    "standard-version": "^9.5.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "dayjs": "^1.11.7",
    "ws": "^8.13.0"
  }
}
